dash==4.3.0
dash-ag-grid==35.2.0
dash-bootstrap-components==2.0.4
dash-core-components==2.0.0
dash-html-components==2.0.0
dash-table==5.0.0
Flask-Compress==1.24
gunicorn==23.0.0
markdown==3.9
numpy==2.0.2
pandas==2.3.3
pyarrow==21.0.0
pytz==2026.2
pyyaml==6.0.3
requests==2.32.5
pytest==8.4.2
ruff==0.15.22           # Linter — pinned so CI installs it and stays reproducible
tomli==2.0.1; python_version < "3.11"   # for scripts/check_dep_floors.py on <3.11

# data/metrics layer (editable siblings). cotmetrics provides the COT index,
# signals, indexer + the legacy ETL/options/scheduler; it depends on cotdata,
# which is not on PyPI so it stays listed explicitly for the editable install.
-e ../cotdata
-e ../cotmetrics[options,scheduler]
